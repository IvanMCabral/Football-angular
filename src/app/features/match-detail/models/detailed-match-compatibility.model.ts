/**
 * Backend compatibility values for detailed match payloads.
 *
 * These literals are persisted/returned by the current backend contract and
 * must remain readable by the frontend, but they are not product-facing names.
 */
export const DETAILED_MATCH_ENGINE_VERSION = 'V24' as const;
export const PLAYER_STATS_DETAILED_DATA_SOURCE = 'V24_DETAIL' as const;

