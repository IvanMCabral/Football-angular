// LIVE-MATCH-F3-UI-LIVE F3.4: test polyfills.
// Mirrors the standard `polyfills-test.ts` Angular CLI generates. Loaded
// by the test target in angular.json (polyfills section) to ensure
// zone.js is set up correctly for the test platform.
import 'zone.js';
import 'zone.js/testing';
